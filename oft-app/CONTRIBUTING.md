# Contributing to OFT

Спасибо за интерес к проекту OFT! 🎉

Этот документ описывает процесс разработки и правила участия в проекте.

## 🚀 Как начать

### 1. Fork репозиторий

Нажмите кнопку "Fork" в правом верхнем углу страницы репозитория.

### 2. Clone ваш fork

```bash
git clone https://github.com/YOUR_USERNAME/oft-app.git
cd oft-app
```

### 3. Установите зависимости

```bash
npm install
```

### 4. Запустите dev сервер

```bash
npm run dev
```

Приложение откроется на `http://localhost:5173`

---

## 🌿 Структура веток

- **`main`** — production-ready код, всегда стабилен
- **`develop`** — development ветка для интеграции фич
- **`feature/название`** — новые фичи
- **`fix/название`** — исправления багов
- **`docs/название`** — обновления документации

---

## 🔄 Процесс разработки

### 1. Создайте feature branch

```bash
# Обновите develop
git checkout develop
git pull origin develop

# Создайте новую ветку
git checkout -b feature/my-awesome-feature
```

### 2. Разрабатывайте

- Пишите чистый, читаемый код
- Следуйте code style проекта
- Добавляйте комментарии где необходимо
- Тестируйте локально

### 3. Коммиты

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

```bash
# Формат: <type>(<scope>): <subject>

# Примеры:
git commit -m "feat(client): добавил фильтр по оборудованию"
git commit -m "fix(trainer): исправил баг с удалением клиента"
git commit -m "docs: обновил README"
git commit -m "refactor(store): оптимизировал селекторы"
```

**Типы коммитов:**
- `feat` — новая фича
- `fix` — исправление бага
- `docs` — документация
- `style` — форматирование (не влияет на код)
- `refactor` — рефакторинг
- `test` — тесты
- `chore` — конфигурация, зависимости

### 4. Push и создайте Pull Request

```bash
git push origin feature/my-awesome-feature
```

Затем создайте Pull Request на GitHub:
1. Перейдите в ваш fork
2. Нажмите "New Pull Request"
3. Выберите `develop` как base branch
4. Опишите изменения
5. Укажите связанные issues (если есть)

---

## 📝 Code Style

### TypeScript

- Используйте строгий режим (`strict: true`)
- Избегайте `any`, используйте `unknown` если необходимо
- Используйте типы из `src/data/models/types.ts`

### React

- Функциональные компоненты + hooks
- Используйте `useState`, `useEffect`, `useMemo`, `useCallback` где уместно
- Избегайте лишних ре-рендеров

### Именование

- **Компоненты**: PascalCase (`ClientCard.tsx`)
- **Функции/переменные**: camelCase (`getClient`, `currentClient`)
- **Константы**: UPPER_SNAKE_CASE (`AI_FEATURES_ENABLED`)
- **Типы/интерфейсы**: PascalCase (`Client`, `WorkoutPlan`)

### Импорты

```typescript
// 1. React и библиотеки
import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';

// 2. Внутренние утилиты
import { useStore } from '../store';
import { ROUTES } from '../router/routes';

// 3. Компоненты
import Button from '../components/ui/Button';
import ClientCard from '../components/trainer/ClientCard';

// 4. Типы
import type { Client } from '../data/models';
```

### Форматирование

- Используйте Prettier (настроен в проекте)
- 2 пробела для отступов
- Точка с запятой в конце строк
- Одинарные кавычки для строк

---

## 🧪 Тестирование

Перед созданием PR проверьте:

### Функциональность

- [ ] Все сценарии работают корректно
- [ ] Нет критических багов
- [ ] Обработаны edge cases

### UI/UX

- [ ] Проверено на mobile (375px)
- [ ] Проверено на tablet (768px)
- [ ] Проверено на desktop (1440px+)
- [ ] Анимации плавные
- [ ] Toast уведомления работают

### Код

- [ ] TypeScript без ошибок (`npm run type-check`)
- [ ] ESLint без критичных warnings (`npm run lint`)
- [ ] Нет `console.log` в production коде
- [ ] Все импорты корректны

### Производительность

- [ ] Нет memory leaks
- [ ] Bundle size не увеличился значительно
- [ ] Lazy loading работает

---

## 📋 Pull Request Checklist

Перед отправкой PR убедитесь:

- [ ] Код следует code style проекта
- [ ] Коммиты следуют Conventional Commits
- [ ] Добавлена документация (если необходимо)
- [ ] Обновлён CHANGELOG.md (если это новая фича)
- [ ] Нет конфликтов с `develop`
- [ ] Все проверки пройдены

---

## 🐛 Сообщение о багах

Создайте Issue с шаблоном:

```markdown
**Описание бага**
Краткое описание проблемы

**Шаги для воспроизведения**
1. Перейти на '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти

**Скриншоты**
Если применимо

**Окружение:**
- Браузер: Chrome 120
- ОС: Windows 11
- Версия: 0.1.0
```

---

## 💡 Предложение фич

Создайте Issue с шаблоном:

```markdown
**Описание фичи**
Что вы хотите добавить

**Проблема, которую решает**
Зачем это нужно

**Предложенное решение**
Как это можно реализовать

**Альтернативы**
Другие варианты решения
```

---

## 📚 Дополнительные ресурсы

- [React Documentation](https://react.dev)
- [TypeScript Handbook](https://www.typescriptlang.org/docs/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Vite Documentation](https://vite.dev)

---

## ❓ Вопросы?

Если у вас есть вопросы:
1. Проверьте документацию в `/docs`
2. Поищите существующие Issues
3. Создайте новый Issue с вопросом

---

## 🙏 Спасибо!

Ваш вклад делает OFT лучше! 🚀
